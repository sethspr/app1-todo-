def todolist():
    user_prompt = "Type add or show: "

    todo_list = []

    while True:
        todo = input(user_prompt)
        (todo.title())
        todo_list.append(todo)

    return todolist(todo_list)

